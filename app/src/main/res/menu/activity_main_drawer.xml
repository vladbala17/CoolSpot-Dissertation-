<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_fav_places"
            android:icon="@drawable/ic_favourites"
            android:title="Favorites" />
        <item
            android:id="@+id/nav_history_places"
            android:icon="@drawable/ic_history"
            android:title="History" />
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_preferences"
            android:title="Preferences" />
    </group>

    <item android:title="Special">
        <menu>
            <item
                android:id="@+id/nav_get_premium"
                android:icon="@drawable/ic_premium"
                android:title="Get premium" />
            <item
                android:id="@+id/nav_rate_app"
                android:icon="@drawable/ic_rating"
                android:title="Rate the app" />
        </menu>
    </item>

</menu>
